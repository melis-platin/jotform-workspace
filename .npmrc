# Hoist the serverless-function deps to the root node_modules so Vercel's function
# bundler can resolve them in this pnpm monorepo. (App packages are unaffected.)
public-hoist-pattern[]=@upstash/*
public-hoist-pattern[]=@vercel/*
